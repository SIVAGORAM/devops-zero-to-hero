apiVersion: v1
kind: Pod
metadata:
  name: dummy-node
spec:
  containers:
    - name: c1
      image: ubuntu
      command: ["/bin/bash", "-c", "while true; do echo hello-devops; sleep 4; done"]
  nodeSelector:
    mynode: demonode
